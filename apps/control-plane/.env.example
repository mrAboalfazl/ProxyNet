DATABASE_URL="postgresql://platform:platform_dev@localhost:5432/platform"
REDIS_URL="redis://localhost:6379"
JWT_SECRET="change-me-in-production-use-a-long-random-string"
JWT_EXPIRES_IN="30d"
APP_PORT=3000
NODE_ENV=development

# Quota token signing secret (separate from JWT)
QUOTA_TOKEN_SECRET="change-me-too-long-random-string"

# KaveNegar SMS credentials. Set these only in the deployment environment.
KAVENEGAR_API_KEY=""
KAVENEGAR_SENDER=""
